<!DOCTYPE html>
<html lang="uk">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Профіль</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                max-width: 600px;
                margin: 50px auto;
                padding: 20px;
            }
            .profile-header {
                text-align: center;
                margin-bottom: 30px;
            }
            .nav-buttons {
                display: flex;
                gap: 10px;
                justify-content: center;
                margin-bottom: 20px;
            }
            .btn {
                padding: 10px 20px;
                text-decoration: none;
                border-radius: 4px;
                cursor: pointer;
            }
            .btn-primary {
                background: #007bff;
                color: white;
            }
            .btn-success {
                background: #28a745;
                color: white;
            }
            .btn-danger {
                background: #dc3545;
                color: white;
            }
            .btn:hover {
                opacity: 0.8;
            }
            .user-info {
                background: #f8f9fa;
                padding: 20px;
                border-radius: 8px;
            }
        </style>
    </head>
    <body>
        <div class="profile-header">
            <h1>Вітаємо, <%= username %>!</h1>
        </div>

        <div class="nav-buttons">
            <a href="/chat" class="btn btn-primary">Перейти до чату</a>
            <a href="/logout" class="btn btn-danger">Вийти</a>
        </div>

        <div class="user-info">
            <h3>Інформація про профіль</h3>
            <p><strong>Ім'я користувача:</strong> <%= username %></p>
            <p><strong>Статус:</strong> Активний</p>
            <p><strong>Доступ до чату:</strong> Увімкнено</p>
        </div>
    </body>
</html>
